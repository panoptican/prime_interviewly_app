.welcome(ng-controller="archStudents" layout="column")
   .studentssheader(layout="row" layout-align="start center" layout-margin="auto" flex="auto")
      md-subheader(class="md-primary")
         h1.md-display-1 Archived Students

      .searchsheader(layout="row" layout-align="start center" layout-margin="auto" flex="auto")
   span(flex="5")
   md-content
      md-card(ng-show="students.length")
         md-data-table-toolbar
            h2(class="md-title") Students
         md-data-table-container
            table(md-data-table md-row-select="selected" md-progress="deferred")
               thead(md-order="query.order" md-trigger="onOrderChange")
                  tr
                     th(name="First Name" order-by="name")
                     th(name="Last Name" order-by="name")
                     th(name="email" order-by="name")
                     th(name="cohort" order-by="name")
                     th(name="unarchive")
               tbody
                  tr(md-auto-select ng-repeat="student in students")
                     td {{student.fName}}
                     td {{student.lName}}
                     td {{student.email}}
                     td {{student.cohort}}
                     td
                        md-button.md-icon-button(ng-click="unarchive(student._id)")
                           md-icon.material-icons.md-dark archive
                           .md-ripple-container